// Re-export the simple auth module
export * from './auth-simple';

// This is a lightweight, production-ready auth system
// It uses JWT tokens stored in secure cookies
// No complex adapters or database driver initialization required
