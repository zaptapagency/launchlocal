{
  "$schema": "https://railway.app/schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install && pnpm build"
  },
  "deploy": {
    "startCommand": "sh -c 'echo DATABASE_URL: $DATABASE_URL && echo AUTH_SECRET length: $(echo $AUTH_SECRET | wc -c) && pnpm db:migrate 2>&1 || echo Migration failed with code $? && sleep 2 && pnpm start'",
    "healthcheckPath": "/",
    "healthcheckTimeout": 120
  }
}
