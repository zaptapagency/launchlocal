{
  "$schema": "https://railway.app/schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install && pnpm build"
  },
  "deploy": {
    "startCommand": "sh -c 'echo Starting migrations... && node node_modules/.bin/drizzle-kit migrate 2>&1; echo; echo Starting Next.js server... && pnpm start'",
    "healthcheckPath": "/",
    "healthcheckTimeout": 120
  }
}
