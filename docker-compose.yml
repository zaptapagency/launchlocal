version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: launchlocal-postgres
    environment:
      POSTGRES_DB: launchlocal
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_INITDB_ARGS: '--encoding=UTF8'
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres']
      interval: 10s
      timeout: 5s
      retries: 5
    command:
      - 'postgres'
      - '-c'
      - 'log_statement=all'
      - '-c'
      - 'log_duration=on'
      - '-c'
      - 'max_connections=200'

volumes:
  postgres_data:
